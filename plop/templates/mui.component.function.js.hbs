import PropTypes from 'prop-types'
import { styled } from "@material-ui/core/styles";
import {{componentName}} from "@material-ui/core/{{componentName}}";
import styles from "components/Mui{{componentName}}/Mui{{componentName}}.module.scss"

const Custom{{componentName}} = styled({{componentName}})(({ theme, ...props }) => ({

}));


Custom{{componentName}}.propTypes = {

}

Custom{{componentName}}.defaultProps = {
  "data-component": {{componentName}} 
}

export default Custom{{componentName}}